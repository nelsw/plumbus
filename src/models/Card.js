export default class Card {

    constructor(name, icon, subtitle, plus, visible, expanded) {
        this.name = name
        this.icon = icon
        this.subtitle = subtitle
        this.plus = plus
        this.visible = visible
        this.expanded = expanded
    }
}